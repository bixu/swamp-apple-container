{
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "singleQuote": false,
    "proseWrap": "preserve"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-import-prefix"]
    }
  },
  "tasks": {
    "test": "deno test --allow-read --allow-write --allow-run --allow-env extensions/drivers/",
    "smoke": "SWAMP_APPLE_CONTAINER_SMOKE=1 deno test --allow-all extensions/drivers/container_test.ts"
  }
}
